"""OASIS-3 pipeline stages.

Importing this package implies the pipeline extra is installed
(``pip install 'brainjar[oasis3-pipeline]'``); plain loader use should
go through ``brainjar.oasis3`` (``get_df_image``, ``get_df_xfeat``,
``LABELS``) and never touch these modules directly.
"""
